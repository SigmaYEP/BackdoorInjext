if game.CoreGui:FindFirstChild("NexusCodeHub") then
    game.CoreGui.NexusCodeHub:Destroy()
end

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local UIS = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "NexusCodeHub"
ScreenGui.Parent = game.CoreGui
ScreenGui.ResetOnSpawn = false

local Frame = Instance.new("Frame")
Frame.Size = UDim2.new(0, 280, 0, 300)
Frame.Position = UDim2.new(0.5, -140, 0.5, -150)
Frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
Frame.BorderSizePixel = 0
Frame.Parent = ScreenGui
Frame.Active = true
Frame.Draggable = true

local UICorner = Instance.new("UICorner", Frame)
UICorner.CornerRadius = UDim.new(0, 12)

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -30, 0, 35)
Title.Position = UDim2.new(0, 10, 0, 0)
Title.BackgroundTransparency = 1
Title.Text = "Nexus Code Hub Universal"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 18
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = Frame

local Close = Instance.new("TextButton")
Close.Size = UDim2.new(0, 25, 0, 25)
Close.Position = UDim2.new(1, -30, 0, 5)
Close.Text = "X"
Close.TextColor3 = Color3.fromRGB(255, 80, 80)
Close.Font = Enum.Font.GothamBold
Close.TextSize = 16
Close.BackgroundTransparency = 1
Close.Parent = Frame
Close.MouseButton1Click:Connect(function()
    Frame.Visible = false
end)

local Footer = Instance.new("TextLabel")
Footer.Size = UDim2.new(1, 0, 0, 20)
Footer.Position = UDim2.new(0, 0, 1, -20)
Footer.BackgroundTransparency = 1
Footer.Text = "Made by KozftYT on YouTube | New features soon"
Footer.TextColor3 = Color3.fromRGB(200, 200, 200)
Footer.Font = Enum.Font.Gotham
Footer.TextSize = 12
Footer.Parent = Frame

local YTButton = Instance.new("TextButton")
YTButton.Size = UDim2.new(1, -20, 0, 25)
YTButton.Position = UDim2.new(0, 10, 1, -45)
YTButton.Text = "KozftYT Channel"
YTButton.TextColor3 = Color3.fromRGB(255, 255, 255)
YTButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
YTButton.Font = Enum.Font.GothamBold
YTButton.TextSize = 14
YTButton.Parent = Frame
YTButton.MouseButton1Click:Connect(function()
    setclipboard("https://www.youtube.com/@kozft")
end)

local function createToggle(text, order, callback)
    local Button = Instance.new("TextButton")
    Button.Size = UDim2.new(1, -20, 0, 35)
    Button.Position = UDim2.new(0, 10, 0, 40 + (order * 40))
    Button.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    Button.TextColor3 = Color3.fromRGB(255, 255, 255)
    Button.Text = text .. ": OFF"
    Button.Font = Enum.Font.GothamBold
    Button.TextSize = 14
    Button.Parent = Frame
    local UIC = Instance.new("UICorner", Button)
    UIC.CornerRadius = UDim.new(0, 8)
    local state = false
    Button.MouseButton1Click:Connect(function()
        state = not state
        Button.Text = text .. (state and ": ON" or ": OFF")
        Button.BackgroundColor3 = state and Color3.fromRGB(60, 120, 60) or Color3.fromRGB(50, 50, 50)
        callback(state)
    end)
    return Button
end

local ESPEnabled = false
local function updateESP()
    for _,plr in pairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer and plr.Character then
            if not plr.Character:FindFirstChild("NexusESP") then
                local hl = Instance.new("Highlight")
                hl.Name = "NexusESP"
                hl.FillTransparency = 1
                hl.OutlineColor = Color3.fromRGB(0, 255, 0)
                hl.Parent = plr.Character
            end
        end
    end
end
createToggle("ESP", 0, function(state)
    ESPEnabled = state
    if not state then
        for _,plr in pairs(Players:GetPlayers()) do
            if plr.Character and plr.Character:FindFirstChild("NexusESP") then
                plr.Character.NexusESP:Destroy()
            end
        end
    end
end)
RunService.Heartbeat:Connect(function()
    if ESPEnabled then updateESP() end
end)

local AimbotEnabled = false
local function getClosestPlayer()
    local closest,dist
    if not LocalPlayer.Character or not LocalPlayer.Character:FindFirstChild("Head") then return nil end
    for _,plr in pairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
            local pos,vis = workspace.CurrentCamera:WorldToViewportPoint(plr.Character.HumanoidRootPart.Position)
            if vis then
                local mag = (Vector2.new(pos.X,pos.Y) - UIS:GetMouseLocation()).Magnitude
                if not dist or mag < dist then
                    closest = plr
                    dist = mag
                end
            end
        end
    end
    return closest
end
RunService.RenderStepped:Connect(function()
    if AimbotEnabled then
        local target = getClosestPlayer()
        if target and target.Character:FindFirstChild("Head") then
            workspace.CurrentCamera.CFrame = CFrame.new(workspace.CurrentCamera.CFrame.Position, target.Character.Head.Position)
        end
    end
end)
createToggle("Aimbot", 1, function(state)
    AimbotEnabled = state
end)

local InfJump = false
UIS.JumpRequest:Connect(function()
    if InfJump and LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid") then
        LocalPlayer.Character:FindFirstChildOfClass("Humanoid"):ChangeState("Jumping")
    end
end)
createToggle("Infinite Jump", 2, function(state)
    InfJump = state
end)

local AntiFlingButton = Instance.new("TextButton")
AntiFlingButton.Size = UDim2.new(1, -20, 0, 35)
AntiFlingButton.Position = UDim2.new(0, 10, 0, 160)
AntiFlingButton.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
AntiFlingButton.TextColor3 = Color3.fromRGB(255, 255, 255)
AntiFlingButton.Text = "Anti-Fling"
AntiFlingButton.Font = Enum.Font.GothamBold
AntiFlingButton.TextSize = 14
AntiFlingButton.Parent = Frame
local UIC = Instance.new("UICorner", AntiFlingButton)
UIC.CornerRadius = UDim.new(0, 8)
AntiFlingButton.MouseButton1Click:Connect(function()
    if LocalPlayer.Character then
        for _,v in pairs(LocalPlayer.Character:GetChildren()) do
            if v:IsA("BasePart") then
                v.CustomPhysicalProperties = PhysicalProperties.new(0,0,0,0,0)
            end
        end
    end
end)
